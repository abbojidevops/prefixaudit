node_modules/
.DS_Store
*.log
.cache/

# Build artefacts — regenerated by `npm run build`, never committed
dist/
*.tgz
*.zip

# Local scratch
.pipeline/
.prefixaudit-baseline.json

# Private business playbook — lives on this disk (the publish gates read it)
# but never in the public repository.
sales/
launch/
BUSINESS.md
GO.md
PROJECT.md
